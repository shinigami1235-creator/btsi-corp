# Archive

Nothing here is read by the build.

`catalogue.json` is the original scrape of btsi.com.ph. It holds each product's
old WordPress address in its `link` field, and the redirects are built from
those the day btsi.com.ph points at this site.

`migrate-data.py` is the one-off that turned that scrape into data/. It has
already run.
